{
  "name": "robank",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix frontend --legacy-peer-deps",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "deploy": "npm run deploy --prefix frontend",
    "cli": "node cli/bin/robank.js"
  }
}
